import 'package:flutter/material.dart';

class BattleFieldScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Battle Field Screen'),
      ),
    );
  }
}
